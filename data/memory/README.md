# 项目创作记忆

这里保存 DSP 自身的创作记忆，不属于 WorkBuddy、Claude 或其他开发工具的运行时记忆。

## 规则

- `style-preferences.json` 是项目创作风格规则的 SSOT。
- 规则按题材匹配，生成故事时自动注入 `story.style`。
- 用户在当前故事中明确指定 `style` 时，明确指定优先，不覆盖项目默认规则。
- 这里保存可复用的项目规则，不保存 API Key、个人隐私或单次任务媒体。
