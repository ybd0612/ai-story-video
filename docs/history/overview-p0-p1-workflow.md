# DSP P0/P1 工作流实施概览

> 🔒 **时点快照（P0/P1 轮次，提交于 2026-09-07）**，只记录当时交付，不作为现状来源。
> 现行事实以 [项目总纲 §2 SSOT](../PROJECT_INDEX.md) 为准；冲突登记见 [总纲 §7](../PROJECT_INDEX.md)。
> 已知过期项：下文「`npm test` 7/7」为当时基线，现已大幅扩充；实时项数与通过状态只看 [总纲 §2.4](../PROJECT_INDEX.md)，本注记不复述数字以免再次过期。

## 已完成

- 基于 Agent-first 设计实施任务状态 schema v2。
- 状态文件支持原子写入、阶段状态、镜头状态和中断恢复识别。
- 增加阶段产物契约、受控路径校验、媒体实体校验和交付 hash 校验。
- 增加本地任务锁，避免同一 job 并发执行。
- 支持 `JOB_ID + RESUME=1` 断点续跑。
- 支持图片和 TTS 的指定镜头补偿，并复用有效图片/音频。
- 无效 `SCENE_ID` 会拒绝执行。
- `render`、`deliver` 已纳入统一阶段状态。
- 当前不建设可视化界面，Agent 仍是唯一控制面。

## 验证

- QA 最终结论：通过。
- `npm test`：7/7 通过。
- `npm run typecheck`：通过。
- Node 脚本语法检查：通过。
- `generate-edge-tts.py` Python 编译检查：通过。
- 无效 `TARGET_SCENE_ID` 实测返回退出码 1，且不生成输出文件。
- 普通 TTS 有效 MP3 复用通过无网络测试。
- 未调用真实 Agnes 或 Edge TTS 服务。

## 关键提交

- `a04b26b`：可恢复视频流水线状态与阶段契约
- `ca54c58`：受控交付和输入路径
- `9977ca8`：媒体复用与流水线产物校验
- `4322d18`：拒绝无效目标图片镜头
- `0c1daf2`：同步工作流方案实施状态

## 注意事项

- 未推送远程仓库。
- 当前 completed 阶段与产物契约不一致时，流水线停止并报告，不静默覆盖。
- 真实 API/视频全链路仍需在具备密钥和实际素材时做低成本集成验证。