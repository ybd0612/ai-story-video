# DSP 项目第二阶段改进记录

> 🔒 **时点快照（2026-09-04，提交 `dcbe048`）**，仅记录该阶段。下文「自动化测试 5 项通过」为当时基线，测试已由后续 P0/P1/E0/E1 轮次扩展到 15 个文件共 37 项，实时基线见 [项目总纲 §2.4](../PROJECT_INDEX.md)。本阶段结论（`--props` 隔离任务故事、运行时校验前移）仍然成立。

## 已完成

- Remotion 渲染通过 `--props` 接收当前任务故事，不再依赖共享 `src/story/currentStory.json`，降低并发任务串线风险。
- 默认预览故事改为代码内的 `SAMPLE_STORY`，避免渲染依赖被忽略的生成文件。
- `validateStory` 改为接收 `unknown`，补齐故事、人物、镜头字段、重复镜头 ID 和时长校验。
- `storyRepository.getStoryFromJson` 进入渲染前执行运行时校验。
- 新增故事校验自动化测试，覆盖合法故事、重复 ID 和非法时长。
- 测试命令改为显式列出测试文件，兼容当前 Node.js 22 环境。
- 同步 README 与工作流文档，说明任务故事通过 `--props` 传入。

## 验证

- `.mjs` 语法检查：通过
- TypeScript `tsc --noEmit`：通过
- 自动化测试：5 项通过，0 失败
- 真实 5 镜头故事 dry-run：通过
- Git 工作树：干净

## 提交

`dcbe048 隔离任务故事并统一运行时校验`

## 后续建议

媒体缓存指纹、视频质量验收、BGM/混音和发布数据结构仍可作为下一阶段独立任务实现；本阶段未调用图片、TTS 或视频渲染服务。
